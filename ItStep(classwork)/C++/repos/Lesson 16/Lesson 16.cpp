#include <iostream>
using namespace std;

//task 1
/*
#define MINIM(X, Y, REZULT) REZULT = (X < Y) ? X : Y
#define MAXM(X, Y, REZULT) REZULT = (X > Y) ? X : Y
#define SQUARE(X) X *= X
#define STYPIN(X, Y, REZULT) for(int i = 0; i < Y; ++i) REZULT *= X
#define IS_PARN(X) (x % 2 == 0) ? 1 : 0
#define IS_NOT_PARN(X) !(IS_PARN(X))
*/

//task 2


int main()
{
	setlocale(0, "");

	int a = 5, b = 2, rezult = 0;

	//task 1
	/*
	MINIM(a, b, rezult);
	cout << "\nРезультат: " << rezult;
	
	MAXM(a, b, rezult);
	cout << "\nРезультат: " << rezult;

	long x = 10;
	SQUARE(x);
	cout << "\nРезультат: " << x;
	
	rezult = 1;
	STYPIN(a, b, rezult);
	cout << "\nРезультат: " << rezult;
	*/

	//task 2


	cout << endl;
	system("pause");
}